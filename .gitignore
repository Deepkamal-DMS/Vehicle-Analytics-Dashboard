# Source data exports (large, not code)
RTO/
Vahan New/
RTO Vehicle Maker/

# Only the Supabase CLI's own local scratch state - everything else
# under supabase/ (config.toml, migrations/, functions/) is real,
# hand-written source for the Import feature and must stay tracked.
supabase/.branches/
supabase/.temp/

# Node
node_modules/
package-lock.json

# Excel lock files
~$*

# Reference screenshots: these carry real dealer figures and the
# repo is public, so they stay local.
ref.jpeg
ref.jpg
ref.png

# Generated locally by tools/build-local-db.js and fetch-scope-tables.js
local/data/

# Never commit the Supabase management token
.env
local/.env

# The sample design mock-up this rebuild was matched against.
# It carries invented figures for years the data does not have, so
# it stays local rather than shipping in a public repo.
Dashboard Semple.png
